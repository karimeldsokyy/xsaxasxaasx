import 'package:flutter/material.dart';

class BacPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bac Page'),
      ),
      body: Center(
        child: Text('This is the Bac Page'),
      ),
    );
  }
}