import 'package:flutter/material.dart';

class RolPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rol Page'),
      ),
      body: Center(
        child: Text('This is the Rol Page'),
      ),
    );
  }
}